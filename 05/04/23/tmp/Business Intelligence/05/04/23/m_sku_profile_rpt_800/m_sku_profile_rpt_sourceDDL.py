# Databricks notebook source
# COMMAND ----------

CREATE DATABASE IF NOT EXISTS DELTA_TRAINING;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.SAP_ATTRIBUTE(PRODUCT_ID INT,
SAP_ATT_TYPE_ID STRING,
SAP_ATT_CODE_ID STRING,
SAP_ATT_VALUE_ID STRING,
DELETE_FLAG STRING,
UPDATE_DT TIMESTAMP,
LOAD_DT TIMESTAMP) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.DM_DEPT_SEGMENTS(SAP_DEPT_ID INT,
CONSUM_ID BIGINT,
CONSUM_DESC STRING,
SEGMENT_ID BIGINT,
SEGMENT_DESC STRING) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.SKU_CONTAINER(CONTAINER_CD STRING,
CONTAINER_DESC STRING,
UPDATE_TSTMP TIMESTAMP,
LOAD_TSTMP TIMESTAMP) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.SAP_ATT_VALUE(SAP_ATT_VALUE_ID STRING,
SAP_ATT_CODE_ID STRING,
SAP_ATT_VALUE_DESC STRING) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.OPT_PRODUCT_RELATIONSHIP_PRE(PRODUCT_NUMBER STRING,
PRODUCT_DESC STRING,
SAP_DEMAND_GROUP STRING,
CUSTOMER_DEMAND_GROUP STRING,
SAP_PRODUCT_LINE STRING,
CUSTOMER_PRODUCT_LINE STRING,
SAP_PRICE_FAMILY STRING,
CUSTOMER_PRICE_FAMILY STRING,
EFFECTIVE_SIZE INT,
SELL_UNITS INT,
UNIT_PRICE INT,
PRICE INT,
COST INT,
IS_NEW STRING,
ERROR_CODE STRING) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.SKU_FLAVOR(FLAVOR_CD STRING,
FLAVOR_DESC STRING,
UPDATE_TSTMP TIMESTAMP,
LOAD_TSTMP TIMESTAMP) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.BRAND_CLASSIFICATION(BRAND_CLASSIFICATION_ID INT,
BRAND_CLASSIFICATION_NAME STRING,
UPDATE_DT DATE,
LOAD_DT DATE) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.SAP_T6WP1T_PRE(PROCUREMENT_RULE_CD STRING,
PROCUREMENT_RULE_DESC STRING) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.GL_CATEGORY(GL_CATEGORY_CD STRING,
GL_CATEGORY_DESC STRING) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.SKU_HAZMAT(RTV_DEPT_CD STRING,
RTV_DESC STRING,
HAZ_FLAG STRING,
AEROSOL_FLAG STRING,
ADD_DT TIMESTAMP,
LOAD_DT TIMESTAMP) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.OWNBRAND(OWNBRAND_FLAG STRING,
OWNBRAND_DESC STRING) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.ZDISCO_MKDN_SCHED(ZDISCO_MKDN_SCHED_ID STRING,
ZDISCO_MKDN_SCHED_DESC STRING) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.SAP_ZTH_WEB_WIP_PRE(RECORD_ID STRING,
MATKL STRING,
WSTAW STRING,
POG_REPLACE STRING,
DISC_SKU_NBR INT,
RTV_DISP STRING,
SELL_THROUGH STRING,
PRES_MGR STRING,
NOTIFY_PRICE STRING,
PRICE_MGR STRING,
KHOP STRING,
COLOR STRING,
SIZE1 STRING,
FLAVOR STRING,
RTV STRING,
DISCIPLINE STRING,
STATUS_CODE STRING,
PLAN_GROUP STRING,
STATELINETACK STRING,
DUE_IN_STORE TIMESTAMP,
QTY_FOR_DC4 STRING,
RDPRF STRING,
APU_QTY INT,
USITEM STRING,
CAITEM STRING,
US_CA_DIR_ITEM STRING,
INLINE STRING,
DPR_SKU_NBR INT,
DPR_PLUS STRING,
DPR_PERC STRING,
DPR_MIN_QTY STRING,
OTB STRING,
BUYER_SUGGEST STRING,
BUYER_A STRING,
BUYER_B STRING,
BUYER_C STRING,
BUYER_D STRING,
BUYER_E STRING,
BUYER_F STRING,
DC8 STRING,
DC9 STRING,
DC10 STRING,
DC12 STRING,
S2920 STRING,
ALLOC STRING,
PRODUCT_CODE STRING,
BASE_NUMBER STRING,
BASE_DESC STRING,
SLT_CAT_CODE STRING,
SUB_PROD_CODE STRING,
SADDLE STRING,
SIZE_CHART_NUM STRING,
CASE_PACK_QTY STRING,
HAZ_MAT STRING,
AEROSOL STRING,
ROLL STRING,
OVER_SIZE STRING,
PSM_LABEL STRING,
OWNS STRING,
BASE_GEN STRING,
DCLR STRING,
DSZE STRING,
DBRD STRING,
DFRT STRING,
STOR STRING,
WEB STRING,
CATL STRING,
DTCD STRING,
DLBL STRING,
PROJECT_ID STRING,
CREATED_BY STRING,
CREATED_TSTMP TIMESTAMP,
LAST_CHANGED_BY STRING,
LAST_CHANGED_TSTMP TIMESTAMP,
DC36 STRING,
S2910 STRING,
S2930 STRING,
AVG_SALES INT,
SUBC STRING,
SPID STRING,
DC38 STRING,
AVG_CATWEB INT,
INIT_BUY STRING,
COPY_SKU_NBR INT,
COPY_SKU_PCT INT,
ALL_STORES STRING,
LIM_STORES STRING,
WEB_STYLE STRING,
WEB_STYLE_TEXT STRING,
WEB_HAZD STRING,
DC41 STRING,
DC14 STRING,
DC16 STRING,
DC18 STRING,
EXP_DAYS STRING,
MHDRZ INT,
DC39 STRING,
DC40 STRING,
DC42 STRING,
COPY_SKU_PCT_SIGN STRING,
PRITEM STRING,
DC43 STRING) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.ZDISCO_SCHED_TYPE(ZDISCO_SCHED_TYPE_ID STRING,
ZDISCO_SCHED_TYPE_CD STRING,
ZDISCO_SCHED_TYPE_DESC STRING) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.SIGN_TYPE(SIGN_TYPE_CD STRING,
SIGN_TYPE_DESC STRING) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.IMPORT(IMPORT_FLAG STRING,
IMPORT_DESC STRING) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.ARTMAS_PRE(ARTICLE_NBR STRING,
ARTICLE_DESC STRING,
ALTERNATE_DESC STRING,
ARTICLE_TYPE_CD STRING,
MERCH_CATEGORY_CD STRING,
ARTICLE_CATEGORY_CD STRING,
ARTICLE_STATUS_CD STRING,
PRIMARY_VENDOR_CD STRING,
BRAND_CD STRING,
PROC_RULE STRING,
FLAVOR_CD STRING,
COLOR_CD STRING,
SIZE_CD STRING,
RTV_CD STRING,
CREATE_DT TIMESTAMP,
CREATED_BY STRING,
UPDATE_DT TIMESTAMP,
UPDATED_BY STRING,
BASE_UOM_CD STRING,
BASE_UOM_ISO_CD STRING,
DOC_NBR STRING,
DOC_SHEET_CNT INT,
WEIGHT_NET_AMT INT,
CONTAINER_REQMT_CD STRING,
TRANSPORT_GROUP_CD STRING,
DIVISION_CD STRING,
GR_GI_SLIP_PRINT_CNT INT,
SUPPLY_SOURCE_CD STRING,
WEIGHT_ALLOWED_PKG_AMT INT,
VOLUME_ALLOWED_PKG_AMT INT,
WEIGHT_TOLERANCE_AMT INT,
VOLUME_TOLERANCE_AMT INT,
VARIABLE_ORDER_UNIT_FLAG STRING,
VOLUME_FILL_AMT INT,
STACKING_FACTOR_AMT INT,
SHELF_LIFE_REM_CNT INT,
SHELF_LIFE_TOTAL_CNT INT,
STORAGE_PCT INT,
VALID_FROM_DT TIMESTAMP,
DELETE_DT TIMESTAMP,
XSITE_STATUS_CD STRING,
XSITE_VALID_FROM_DT TIMESTAMP,
XDIST_VALID_FROM_DT TIMESTAMP,
TAX_CLASS_CD STRING,
CONTENT_UNIT_CD STRING,
NET_CONTENTS_AMT INT,
CONTENT_METRIC_UNIT_CD STRING,
NET_CONTENTS_METRIC_AMT INT,
COMP_PRICE_UNIT_AMT INT,
GROSS_CONTENTS_AMT INT,
ITEM_CATEGORY STRING,
FIBER_SHARE1_PCT INT,
FIBER_SHARE2_PCT INT,
FIBER_SHARE3_PCT INT,
FIBER_SHARE4_PCT INT,
FIBER_SHARE5_PCT INT,
TEMP_SKU STRING,
COPY_SKU STRING,
OLD_ARTICLE_NBR STRING,
BASIC_MATERIAL STRING,
RX_FLAG STRING,
SEASONALITY STRING,
IDOC_NUMBER STRING,
LOAD_TSTMP TIMESTAMP,
UPDATE_TSTMP TIMESTAMP) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.MERCHCAT_ORG(SAP_CATEGORY_ID INT,
SAP_CLASS_ID INT,
SAP_DEPT_ID INT,
MERCH_GL_CATEGORY_CD STRING,
MERCH_GL_CATEGORY_DESC STRING,
CATEGORY_ANALYST_ID INT,
CATEGORY_ANALYST_NM STRING,
CATEGORY_REPLENISHMENT_MGR_ID INT,
CATEGORY_REPLENISHMENT_MGR_NM STRING,
UPDATE_TSTMP TIMESTAMP,
LOAD_TSTMP TIMESTAMP) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.SAP_ZTH_WEB_RECORD_PRE(RECORD_ID STRING,
AVAIL_ON_WEB STRING,
LIFNR STRING,
IDNLF STRING,
BRAND STRING,
BUYER STRING,
PUR_GRP STRING,
ART_TYP STRING,
ART_CAT STRING,
SHIPPER STRING,
CORP_BRAND STRING,
PACK_TYPE STRING,
COUNTRY_OF_ORIGI STRING,
CODE_DATE_IND STRING,
EXP_MONTH STRING,
US_LOC STRING,
SITE_SPECIFIC_C STRING,
COST INT,
CURR_UNIT STRING,
COST_PER STRING,
ORDER_UNIT STRING,
LEAD_TIME INT,
DIRECT_STORE STRING,
TAX_CLASS STRING,
SADDLE STRING,
HAZARDOUS STRING,
AEROSOL STRING,
DATE_AVAILABLE STRING,
IMPORT STRING,
NET_CONTENTS INT,
CONTENT_UNIT STRING,
SP_HANDLE STRING,
LEGAL_IN_CA STRING,
CA_STUFFED STRING,
INGREDIENTS STRING,
EPA STRING,
ALL_STATES STRING,
SKU_NBR INT,
EMAIL STRING,
CHANGE_REC STRING,
CHANGE_FLAGS STRING,
INNER_PACK_TYPE STRING,
ARTILCE_SUB_CAT STRING,
DATE_AVAIL TIMESTAMP,
CREATED_BY STRING,
CREATED_TSTMP TIMESTAMP,
LAST_CHANGED_BY STRING,
LAST_CHANGED_TSTMP TIMESTAMP,
DATBI TIMESTAMP,
SUB_DATE TIMESTAMP,
RTV STRING,
NESTLE_EDI STRING) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.BRAND(BRAND_CD STRING,
BRAND_NAME STRING,
DELETE_FLAG INT,
UPDATE_DT DATE,
LOAD_DT DATE) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.MERCHDEPT_ORG(SAP_DEPT_ID INT,
SAP_DEPT_DESC STRING,
BUS_UNIT_ID INT,
BUS_UNIT_DESC STRING,
BUYER_ID INT,
BUYER_NM STRING,
CA_BUYER_ID INT,
CA_BUYER_NM STRING,
CA_DIRECTOR_ID INT,
CA_DIRECTOR_NM STRING,
CA_MANAGED_FLG STRING,
DIRECTOR_ID INT,
DIRECTOR_NM STRING,
GROUP_ID INT,
GROUP_DESC STRING,
PRICING_ROLE_ID INT,
PRICING_ROLE_DESC STRING,
SEGMENT_ID INT,
SEGMENT_DESC STRING,
SVP_ID INT,
SVP_NM STRING,
VP_ID INT,
VP_DESC STRING,
VP_NM STRING,
CA_VP_ID INT,
CA_VP_NM STRING,
LOAD_DT TIMESTAMP) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.OPT_ASSOCIATED_PRODUCT(PRODUCT_ID INT,
RELATIONSHIP STRING,
METRIC STRING,
DIFF INT,
ASSOC_PROD STRING,
DELETE_OPTION STRING,
UPDATE_DT TIMESTAMP,
LOAD_DT TIMESTAMP) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.VALUATION_CLASS(VALUATION_CLASS_CD STRING,
VALUATION_CLASS_DESC STRING) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.ARTICLE_CATEGORY(ARTICLE_CATEGORY_ID INT,
ARTICLE_CATEGORY_CD STRING,
ARTICLE_CATEGORY_DESC STRING) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.SKU_STATUS(STATUS_ID STRING,
STATUS_NAME STRING) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.BUYER(BUYER_ID STRING,
BUYER_NAME STRING) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.HTS(HTS_CODE_ID BIGINT,
HTS_CODE_DESC STRING) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.COUNTRY(COUNTRY_CD STRING,
COUNTRY_NAME STRING) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.SAP_CLASS(SAP_CLASS_ID INT,
SAP_CLASS_DESC STRING,
SAP_DEPT_ID INT) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.SAP_CATEGORY(SAP_CATEGORY_ID INT,
SAP_CATEGORY_DESC STRING,
SAP_CLASS_ID INT,
GL_CATEGORY_CD STRING,
SAP_PRICING_CATEGORY_ID INT,
UPD_TSTMP TIMESTAMP) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.SKU_PROFILE(PRODUCT_ID INT,
SKU_NBR INT,
SKU_TYPE STRING,
PRIMARY_UPC_ID BIGINT,
STATUS_ID STRING,
SUBS_HIST_FLAG STRING,
SUBS_CURR_FLAG STRING,
SKU_DESC STRING,
ALT_DESC STRING,
SAP_CATEGORY_ID INT,
SAP_CLASS_ID INT,
SAP_DEPT_ID INT,
SAP_DIVISION_ID INT,
PRIMARY_VENDOR_ID BIGINT,
PARENT_VENDOR_ID BIGINT,
COUNTRY_CD STRING,
IMPORT_FLAG STRING,
HTS_CODE_ID BIGINT,
CONTENTS INT,
CONTENTS_UNITS STRING,
WEIGHT_NET_AMT INT,
WEIGHT_UOM_CD STRING,
SIZE_DESC STRING,
BUM_QTY FLOAT,
UOM_CD STRING,
UNIT_NUMERATOR FLOAT,
UNIT_DENOMINATOR FLOAT,
BUYER_ID STRING,
PURCH_GROUP_ID INT,
PURCH_COST_AMT INT,
NAT_PRICE_US_AMT INT,
TAX_CLASS_ID STRING,
VALUATION_CLASS_CD STRING,
BRAND_CD STRING,
BRAND_CLASSIFICATION_ID INT,
OWNBRAND_FLAG STRING,
STATELINE_FLAG STRING,
SIGN_TYPE_CD STRING,
OLD_ARTICLE_NBR STRING,
VENDOR_ARTICLE_NBR STRING,
INIT_MKDN_DT TIMESTAMP,
DISC_START_DT TIMESTAMP,
ADD_DT TIMESTAMP,
DELETE_DT TIMESTAMP,
UPDATE_DT TIMESTAMP,
FIRST_SALE_DT TIMESTAMP,
LAST_SALE_DT TIMESTAMP,
FIRST_INV_DT TIMESTAMP,
LAST_INV_DT TIMESTAMP,
LOAD_DT TIMESTAMP,
BASE_NBR STRING,
BP_COLOR_ID STRING,
BP_SIZE_ID STRING,
BP_BREED_ID STRING,
BP_ITEM_CONCATENATED STRING,
BP_AEROSOL_FLAG INT,
BP_HAZMAT_FLAG INT,
CANADIAN_HTS_CD STRING,
NAT_PRICE_CA_AMT INT,
NAT_PRICE_PR_AMT INT,
RTV_DEPT_CD STRING,
GL_ACCT_NBR INT,
ARTICLE_CATEGORY_ID INT,
COMPONENT_FLAG STRING,
ZDISCO_SCHED_TYPE_ID STRING,
ZDISCO_MKDN_SCHED_ID STRING,
ZDISCO_PID_DT TIMESTAMP,
ZDISCO_START_DT TIMESTAMP,
ZDISCO_INIT_MKDN_DT TIMESTAMP,
ZDISCO_DC_DT TIMESTAMP,
ZDISCO_STR_DT TIMESTAMP,
ZDISCO_STR_OWNRSHP_DT TIMESTAMP,
ZDISCO_STR_WRT_OFF_DT TIMESTAMP) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.VENDOR_PROFILE(VENDOR_ID BIGINT,
VENDOR_NAME STRING,
VENDOR_TYPE_ID INT,
VENDOR_NBR STRING,
LOCATION_ID INT,
SUPERIOR_VENDOR_ID BIGINT,
PARENT_VENDOR_ID BIGINT,
PARENT_VENDOR_NAME STRING,
PURCH_GROUP_ID BIGINT,
EDI_ELIG_FLAG STRING,
PURCHASE_BLOCK STRING,
POSTING_BLOCK STRING,
DELETION_FLAG STRING,
VIP_CD STRING,
INACTIVE_FLAG STRING,
PAYMENT_TERM_CD STRING,
INCO_TERM_CD STRING,
ADDRESS STRING,
CITY STRING,
STATE STRING,
COUNTRY_CD STRING,
ZIP STRING,
CONTACT STRING,
CONTACT_PHONE STRING,
PHONE STRING,
PHONE_EXT STRING,
FAX STRING,
RTV_ELIG_FLAG STRING,
RTV_TYPE_CD STRING,
RTV_FREIGHT_TYPE_CD STRING,
INDUSTRY_CD STRING,
LATITUDE INT,
LONGITUDE INT,
TIME_ZONE_ID STRING,
ADD_DT TIMESTAMP,
UPDATE_DT TIMESTAMP,
LOAD_DT TIMESTAMP) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.TAX_CLASS(TAX_CLASS_ID STRING,
TAX_CLASS_DESC STRING) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.SAP_DIVISION(SAP_DIVISION_ID INT,
SAP_DIVISION_DESC STRING,
MERCH_SVP_ID INT,
MERCH_VP_ID INT) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.SAP_DEPT(SAP_DEPT_ID INT,
SAP_DEPT_DESC STRING,
SAP_DIVISION_ID INT,
MERCH_DIVISIONAL_ID INT,
BUYER_ID STRING) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.PURCH_GROUP(PURCH_GROUP_ID INT,
PURCH_GROUP_NAME STRING,
DVL_MGR_ID INT,
REPLEN_MGR_ID INT) USING DELTA;

CREATE TABLE IF NOT EXISTS DELTA_TRAINING.PRIMARY_VENDOR(PRIMARY_VENDOR_ID BIGINT,
PRIMARY_VENDOR_NAME STRING) USING DELTA;